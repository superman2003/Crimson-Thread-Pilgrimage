# 全章节剧情 NPC 与视听数据阶段交付

生成时间：2026-06-15 22:55:52.193  
负责人：Agent-0  
参与 Agent：Agent-Data-Chapters、Agent-AV-Manifest、Agent-QA-Policy  
范围：CH02-CH06 章节 JSON、campaign 总览、AV manifest、运行时章节注册、免费开源素材边界校验。

## 结果

CH02-CH06 已从策划文档推进为可校验章节数据；campaign 总览、详细章节 JSON、AV manifest 已统一到最终章节线：

1. 苔钟庭
2. 铸雨渠
3. 盐白书库
4. 断弦温室
5. 黑曜巡礼道
6. 无声王冠

当前 CH01 仍是可玩闭环；CH02-CH06 已具备 NPC、故事线、房间、敌人点、Boss、门禁、收集物、结局线索、开放素材映射和视听 beats 的数据基础，并能被 Godot 主场景发现。

## 改动

- 新增 `godot/data/chapters/ch02_rain_foundry_canal.json` 到 `ch06_silent_crown_core.json`。
- 新增 `tools/validate_chapter_files.py`，校验后续章节 JSON 的字段完整度和免费/开源素材边界。
- 新增 `godot/data/audio_visual_manifest.json`，覆盖 8 个必需运行时事件、CH01-CH06 章节视听 beats、本地音频和后续免费音频候选。
- 新增 `tools/validate_audio_visual_manifest.py`。
- 新增 `tools/validate_story_runtime_consistency.py`，校验 campaign、章节文件和 AV manifest 不再分叉。
- 新增 `godot/scripts/chapter_registry_validation.gd`，验证 Godot 主场景可发现 CH02-CH06 章节数据。
- 更新 `godot/data/campaign_chapters.json`，统一章节 ID/名称/目标/Boss/NPC/视听口径。
- 更新 `godot/scripts/main_level.gd`，运行时加载 `res://data/chapters` 和 `audio_visual_manifest.json`。
- 更新 `godot/scripts/demo_validation.gd`，要求主场景加载运行时视听事件和 6 章 AV beats。
- 更新 `tools/validate_open_asset_policy.py`，覆盖后续章节 JSON 和 AV manifest。
- 新增 `docs/plan/20260615224753081_全章节内容与视听验收清单.md`。

## 验证

- `python .\tools\validate_open_asset_policy.py`：PASS。
- `python .\tools\validate_gothicvania_assets.py`：PASS。
- `python .\tools\validate_itch_enemy_assets.py`：PASS。
- `python .\tools\validate_campaign_chapters.py`：PASS，`chapters=6 npcs=23 enemy_concepts=34`。
- `python .\tools\validate_chapter_files.py`：PASS，`files=5 rooms=37 npcs=20 enemy_points=26`。
- `python .\tools\validate_audio_visual_manifest.py`：PASS，`runtime_keys=8 chapters=6 local_audio=14 future_free_candidate=25`。
- `python .\tools\validate_story_runtime_consistency.py`：PASS。
- Godot `chapter_registry_validation.gd`：PASS，runtime discovers CH02-CH06.
- Godot `audio_visual_validation.gd`：PASS。
- Godot `combat_validation.gd`：PASS。
- Godot `demo_validation.gd`：PASS。
- `npm test`：PASS。

## 风险与后续

- CH02-CH06 当前是完整数据配置，不是逐章可玩场景；下一步应从 CH02 铸雨渠开始做最小可玩闭环。
- CH02-CH06 的专属 BGM 和部分章节音效仍标记为 `future_free_candidate`，接入前必须补 CC0/Public Domain/Apache-2.0 compatible 或等价免费商用来源。
- 旧 itch 同族素材仍只允许作为友方 NPC 静态图；敌人/Boss 禁止引用。
