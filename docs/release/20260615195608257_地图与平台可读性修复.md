# 地图与平台可读性修复

时间：2026-06-15 19:56:08

## 修复内容

- 地图面板扩大并加深背景遮罩，避免和场景背景混在一起。
- 房间块增加描边，当前房间使用更强 cyan 边框。
- 玩家位置条加高加粗，钥匙、机关、Boss 点位增加描边。
- 可走平台从半透明块改为暗底、主体、顶部高亮、底部暗边的多层结构。
- 地图刷新从延迟 `queue_free()` 改为即时 `free()`，避免节点重名导致新标记不稳定。

## 验证

- `map_readability_validation.gd`：通过。
- `inventory_map_validation.gd`：通过。
- `metsys_validation.gd`：通过。
- `demo_validation.gd`：通过。
- `validate_demo_geometry.py`、`validate_level_pacing.py`、`validate_background_layers.py`：通过。
