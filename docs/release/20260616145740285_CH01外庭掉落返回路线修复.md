# CH01 外庭掉落返回路线修复

## 结果

已修复第一章外庭段从上层跳下后难以回到主路线的问题。

## 改动

- `godot/data/demo_ch01_moss_bell_court.json`
  - 新增 `outer_return_lip` 平台，连接 `outer_drop` 到 `gear_lift`。
  - 新增 `gear_lift_return_step` 平台，连接 `gear_lift` 到 `moss_steps_a`。
  - 新增 `ch01_outer_court_return` 跑酷段，把外庭掉落恢复路线显式写入关卡数据。

- `godot/scripts/demo_validation.gd`
  - 新增外庭返回路线断言，检查四段跳跃链路都在可达范围内。

## 验证

- `node` JSON 解析：通过，`platforms=26`，`parkour=3`。
- `D:\Godot\godot.cmd --headless --path .\godot --script res://scripts/demo_validation.gd`
  - 通过：`DEMO_VALIDATION_PASS`
- `D:\Godot\godot.cmd --headless --path .\godot --script res://scripts/all_chapters_demo_validation.gd`
  - 通过：`ALL_CHAPTERS_DEMO_VALIDATION_PASS chapters=6 complete=true`

## 风险

- 新增的是小平台，不改变敌人数量、Boss 流程和章节出口。
- 视觉上会比之前更密一点，但能明显避免“跳下去回不来”的死路感。
